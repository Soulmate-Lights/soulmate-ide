@tailwind base;
@tailwind components;
@tailwind utilities;

#loading {
  display: flex;
  align-items: center;
  justify-content: center;
  height: 100vh;
  flex-grow: 1;
}

.loading-spinner {
  width: 40px;
  height: 40px;
  animation: spin 3s linear infinite;
  margin: auto;
}
